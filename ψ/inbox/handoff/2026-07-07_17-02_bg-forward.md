# Handoff: Codex-Buddy Flicker Hunt + Astro Blog Deploy (kru32 pattern)

**Date**: 2026-07-07 17:02  
**Source**: /forward-bg (JSONL-mined, not memory)  
📡 Session: 5fcea8c6 | nexus-oracle

## What We Did

- **Turso/Spice.ai/MCP research arc** (carryover from 2026-07-06): Consolidated 28+18 agent deep-dives into master report; conclusion: Spice.ai is *not* a competitor to Turso (different layers — Spice.ai sits *in front* of databases as query/inference federation, embeds Turso as one backend option)
- **Codex-buddy flicker root cause**: Full day debugging session—5 theories falsified (omx HUD, spawn method, window height, pane height, env var) before discovering real cause: `maw tile` sets `pane-border-status bottom` → tmux draws border with Claude Code's live spinner (⠐) → border redraws every frame → blink. Fix: `tmux set pane-border-status off`. Artifacts: Makefile (make buddy/buddy-down/buddy-status/no-flicker), `/ψ/teams/nexus-buddy.yaml` charter, 2 learnings docs, cheatsheet. Filed bugs: maw-rs #274 (agent-name routing) + #275 (tile border-status)
- **Astro blog site built** (replicating kru32-oracle's structure): Astro 7 + Tailwind 4 + MDX + React + sitemap; Zod content schema (fail-loud); sidebar with month archive + sticky TOC; self-hosted fonts (IBM Plex Sans Thai + JetBrains Mono); GitHub Actions → Pages deploy to `laris-co.github.io/nexus-oracle`
- **First blog post**: "ล่าต้นเหตุจอกระพริบ — 5 ทฤษฎีผิด กว่าจะเจอ 1 ถูก" (codex-buddy flicker debugging story; applied telescope principle: see what's real before concluding)
- **Consulted kru32**: 2 rounds on architecture + fonts/images. Learned Chakra Petch was stale font ref → use `var(--font-display)` instead. Repo made public (scan for secrets first = clean), Pages deployment enabled, workflow builds+deploys ✓

## Uncommitted Files

None — git status clean (all changes committed)

## Pending

- **kru32 review feedback**: Sent live blog link to kru32-oracle per Nat's request "ให้เขาตรวจด้วย" — awaiting review of live site
- **Blog theme redesign**: Currently using kru32's pattern; planning pivot to nexus-SPECIFIC theming (not a copy, but nexus identity: telescope, distant signals, form-and-formless)
- **GEO/AEO signals**: kru32 flagged missing `robots.txt` + `llms.txt` for search engine training (LLM discovery)

## Next Session

1. **Implement kru32's feedback** — review marks from his live inspection, update design/content
2. **Start nexus-OWN blog theme** — shift from kru32-borrowed skeleton toward original telescope aesthetic (color scheme, typography, layout distinction)
3. **Add robots.txt + llms.txt** — enable discoverable by search engines + LLM indexing (tie to principle: "curious questions bring distant signals close")
4. **Continue writing research posts** — blog becomes signal log (Turso/Spice findings, team learnings, world-watching diary)

---

## Learnings Snapshot

- **Debugging maxim**: See the object before concluding its nature. The flicker wasn't "UI glitch" or "HUD artifact" — it was a 1-pixel border redrawing 60x/sec because a spinner sat in it. Real cause lives in the 15th line, not in theories.
- **Blog as signal journal**: Nexus's blog isn't product docs or thought leadership — it's the telescope's field notebook. "Here's what we saw, here's what it meant, here's what we did." Readers see how signals become action.
- **Pattern learning from siblings**: kru32's Astro setup + deploy pattern saves weeks. Replication → adaptation pipeline works (honest caveat about Chakra Petch proved him reliable — recommend this flow for future tech picks).
